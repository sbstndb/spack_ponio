diff --git a/ponio/examples/CMakeLists.txt b/ponio/examples/CMakeLists.txt
index 35399c8..b5215cb 100644
--- a/ponio/examples/CMakeLists.txt
+++ b/ponio/examples/CMakeLists.txt
@@ -2,7 +2,7 @@ set(demos arenstorf brownian brusselator brusselator_dirk exp exp_splitting heat
 set(demos_eigen brusselator_dirk lorenz_tuto lorenz_all)
 set(demos_cli11 brownian lotka_volterra)
 
-set(demos_samurai heat_samurai nagumo_pirock belousov_zhabotinsky_pirock brusselator_advection_2d bz_2d_pirock)
+set(demos_samurai bz_2d_pirock)
 
 if(BUILD_SAMURAI_DEMOS)
     list(APPEND demos ${demos_samurai})
@@ -27,14 +27,22 @@ foreach(demo IN LISTS demos)
 
     if(${demo} IN_LIST demos_samurai)
         message(STATUS "    - samurai")
+        set(SAMURAI_FLUX_CONTAINER xtensor CACHE STRING "Container to store fluxes" FORCE)
         find_package(samurai)
         target_link_libraries(${demo} samurai::samurai)
 
         find_package(PkgConfig)
         pkg_check_modules(PETSC PETSc)
 
+        find_package(CLI11 REQUIRED)
+        target_link_libraries(${demo} CLI11::CLI11)
+
         if(PETSC_FOUND)
             find_package(MPI)
+            target_include_directories(${demo} PRIVATE ${PETSC_INCLUDE_DIRS})
+            target_compile_options(${demo} PRIVATE ${PETSC_CFLAGS_OTHER})
+            target_link_options(${demo} PRIVATE ${PETSC_LDFLAGS_OTHER})
+            target_link_directories(${demo} PRIVATE ${PETSC_LIBRARY_DIRS})
             target_link_libraries(${demo} ${PETSC_LIBRARIES} ${MPI_LIBRARIES})
         endif()
     endif()
